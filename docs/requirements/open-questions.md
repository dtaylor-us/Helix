# Open Questions

1. Which production hosting provider should be selected?
2. Which production authentication provider should be integrated behind the auth boundary?
3. Should backend and web be deployed together or separately?
4. Should JPA remain preferred over Spring Data JDBC after data complexity grows?
5. What offline conflict-resolution policy is preferred for multi-device editing?
6. Which embedding model and provider policy should be used for semantic retrieval?
7. What default retention windows should apply to temporary and derived memory?
