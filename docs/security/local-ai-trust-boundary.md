# Local AI Trust Boundary

- Local model endpoint is treated as an external processor from security perspective.
- User controls whether endpoint is configured.
- Sensitive context should be minimized and explicitly controlled.
