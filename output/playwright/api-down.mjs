import playwright from '../../apps/web/node_modules/playwright/index.js';import fs from'node:fs/promises';const{chromium}=playwright;const browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}});const events=[];page.on('console',m=>events.push({type:m.type(),text:m.text()}));page.on('requestfailed',r=>events.push({type:'failed',url:r.url(),failure:r.failure()}));await page.goto('http://localhost:5173/today');await page.waitForTimeout(1500);const text=await page.locator('main').innerText();await page.screenshot({path:new URL('api-down.png',import.meta.url).pathname,fullPage:true});const result={text,clearError:/error|couldn't|unable|try again|unavailable|failed/i.test(text),events};await fs.writeFile(new URL('api-down.json',import.meta.url),JSON.stringify(result,null,2));console.log(JSON.stringify(result,null,2));await browser.close();
