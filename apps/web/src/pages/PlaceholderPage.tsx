export function PlaceholderPage({ title }: { title: string }) {
  return (
    <section className="card">
      <h2>{title}</h2>
      <p>This area is intentionally scaffolded and will be expanded in future increments.</p>
    </section>
  )
}
